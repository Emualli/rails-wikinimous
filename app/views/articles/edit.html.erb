<%= form_for(@article) do |a| %>
  <%= a.label :title %><br>
  <%= a.text_field :title %>
  <br>
  <%= a.label :content %><br>
  <%= a.text_area :content, {col: 10} %><br>

  <%= a.submit %>
<% end %>
